# NovaCare 用户交互设计方案
> 基于 One UI 10 设计规范 · 针对 NovaCare v0.6.0-alpha 问题诊断
> 2026-09-20

---

## 一、现状问题诊断

### 1.1 核心体验断层

NovaCare 当前的最大 UX 问题：**用户不知道点下去会发生什么**。

| 场景 | 现在 | 应当 |
|------|------|------|
| 首页"一键释放"按钮 | 直接跑扫描，无任何交代 | 告诉用户"接下来会扫什么、不会动什么" |
| 扫描结果 | 一次性给完整清单 | 先给结论（能省多少），再展开详情 |
| 清理执行 | 点完就消失 | 执行过程可见可中断，完成告知释放了多少 |
| 权限引导 | 文字说明 | 视觉引导卡片 + 一键直达 + 回到前台自动重查 |
| 引擎降级 | 静态 banner | 动态说明现在能做什么 / 不能做什么 |
| 空状态 | "没有发现可安全清理的项目" | 分原因（真干净 / 权限不足 / 引擎不可用）给出不同引导 |

### 1.2 One UI 10 设计规范对照

**核心原则**：化繁为简 + 智慧赋能
**动画哲学**：Basic Path Interpolator `[0.22, 0.25, 0.00, 1.00]`（快启动慢停止）；时长 100-500ms
**信息层级**：大标题把主内容推向屏幕中央（大拇指区）；元素放在下半屏
**交互反馈**：即时触觉反馈；每个动作有视觉确认

---

## 二、首屏重构方案（HomeScreen）

### 2.1 信息层级重排

**现在的**：健康分圆环 → 6 个 stat 卡 → 工具栏 → 底栏
**应当的**：

```
┌─────────────────────────────────┐
│  NovaCare          v0.6.0       │  ← 引擎版本号（轻量）
├─────────────────────────────────┤
│  权限引导卡片（按重要性排序）    │  ← 缺失时立即可见
│  ⚠️ 使用情况访问  [开启]        │
│  ⚠️ 所有文件访问  [开启]        │
├─────────────────────────────────┤
│  内核降级卡片（引擎不可用时）    │
│  🔴 扫描不可用，当前仅显示真实读数 │
├─────────────────────────────────┤
│  ┌─────────────────────────┐    │
│  │  本次可释放              │    │  ← 价值前置
│  │  1.2 GB                 │    │  ← 全应用最大字号
│  │  确定安全 800 MB          │    │
│  │  需确认 400 MB（重复文件）│    │
│  │                         │    │
│  │  [一键释放]              │    │  ← 唯一的强调色 CTA
│  └─────────────────────────┘    │
├─────────────────────────────────┤
│  [深度清理]        [冻结应用]  │  ← 两个动词动作卡
├─────────────────────────────────┤
│  存储  60%  ████████░░░░░  │
│  可用 7.8 GB · 已装 156 个应用│  ← 单行细字设备实况
└─────────────────────────────────┘
```

### 2.2 首次使用引导流（Onboarding）

**触发条件**：用户首次打开 + 检测到缺失权限

```
步骤 1/3：为什么需要这个权限
步骤 2/3：按下一个按钮（直达系统设置）
步骤 3/3：回到应用自动检测（LifecycleEventObserver）
```

One UI 10 规范：动画时长 100-500ms，使用 Basic Path Interpolator

### 2.3 扫描前的"预告"弹层

用户点"一键释放"后**不立即跑扫描**，先展示：

```
┌─────────────────────────────────┐
│  NovaCare 正在读取你的设备…      │
│                                 │
│  接下来会扫描：                 │
│  ✓ 应用缓存                     │
│  ✓ 卸载残留                     │
│  ✓ 重复文件（按内容哈希精确匹配）│
│  ✓ 缩略图缓存                   │
│                                 │
│  不会动你的：                    │
│  ✗ 照片和视频                   │
│  ✗ 文档和下载                   │
│  ✗ 聊天记录                     │
│                                 │
│  [开始扫描]        [取消]       │
└─────────────────────────────────┘
```

**设计依据**：One UI 10 "Task-Centered Experience"——在执行前给出任务边界，减少用户焦虑。

---

## 三、清理页交互升级（CleanScreen）

### 3.1 结果的"先结论后详情"结构

**现在**：扫描完成 → 立即展示完整清单
**应当**：

```
步骤 1：概览卡（最大数字 + 三个分项）
┌─────────────────────────────────┐
│  可清理    确定安全      需确认    │
│  1.2 GB     800 MB    400 MB     │
│  [立即执行] [查看详情 ▼]           │
└─────────────────────────────────┘

步骤 2：点击"查看详情"后，展开清单
（使用 AnimatedVisibility，300ms ease-out）
```

### 3.2 执行过程可见化

**现在**：点"执行" → 转圈 → 结果
**应当**：

```
执行中（Progress）
┌─────────────────────────────────┐
│  ◐ 正在清理…       3 / 12 项     │
│  ████████░░░░░░░░░░  25%      │
│                                 │
│  ✓ 微信缓存  180 MB              │
│  ✓ 抖音缩略图  92 MB             │
│  ⟳ 正在处理：抖音缓存…           │
└─────────────────────────────────┘
```

**执行完成（Result）**
```
┌─────────────────────────────────┐
│  ✓ 成功释放  1.07 GB             │
│                                 │
│  成功 10 项 · 失败 0 项          │
│  需要手动 2 项（已引导至设置页）   │
│                                 │
│  已移入回收站，7 天内可撤销       │
│  [完成]        [再扫一次]        │
└─────────────────────────────────┘
```

### 3.3 重复文件的专属交互

**问题**：重复文件现在是 CAUTION，但 UI 上和普通清理项一样展示。

**应当**：单独一个"重复文件"区块，展示配对关系：

```
┌─────────────────────────────────┐
│  重复文件                ⚠️ 需确认  │
│  找到 3 组重复，共节省 420 MB     │
│                                 │
│  [图片_001.jpg]  ←  [图片_001.jpg]│  ← 配对展示
│  同一张照片出现 2 次，删任一个均可  │
│  □ 保留左侧    ■ 删除右侧        │
│                                 │
│  [展开全部 3 组]                  │
└─────────────────────────────────┘
```

---

## 四、冻结页交互升级（FreezeScreen）

### 4.1 已冻结/未冻结的状态区分

**问题**：FreezeViewModel 里 `_frozen` 状态和 UI 渲染没有在列表层面体现。

**应当**：列表每项右上角有状态标签：

```
┌─────────────────────────────────┐
│  [🌙 已冻结]  微信       占用 230 MB │
│  [   ]      抖音       占用 180 MB │  ← 已冻结的不在候选列表重复出现
└─────────────────────────────────┘
```

### 4.2 Shizuku 权限引导

**问题**：Shizuku 是否可用用户完全无感知。

**应当**：

```
首次进入冻结页时检测：
┌─────────────────────────────────┐
│  一键冻结需要 Shizuku            │
│  Shizuku 让你用 ADB 命令冻结应用  │
│  比系统设置页更快更简单          │
│                                 │
│  [下载 Shizuku]   [仅用引导模式] │
└─────────────────────────────────┘
```

---

## 五、动画与 Motion 设计规范

### 5.1 动画时长与节奏

| 场景 | 时长 | 曲线 |
|------|------|------|
| 按钮点击反馈 | 100 ms | ease-out |
| 页面元素进入 | 300 ms | ease-in-out |
| 展开/收起 | 250 ms | ease-in-out |
| 执行进度条 | 持续 | linear |
| 列表项飞入 | 200 ms | stagger 30ms |
| 弹层出现 | 200 ms | ease-out（放大+淡入） |
| 弹层消失 | 150 ms | ease-in（缩小+淡出） |

### 5.2 Micro-interaction 清单

| 动作 | 反馈 |
|------|------|
| 点击"一键释放" | 按钮缩放 0.95x + 轻微震动（如果有） |
| 扫描完成 | Hero 区域数字从 0 滚动到目标值（计数动画） |
| 勾选一个清理项 | 勾选框填充动画 + 数字实时更新 |
| 执行失败 | 整行轻微抖动 + 红色边框 |
| 权限引导卡展开 | Accordion 展开 + 图标旋转 90° |
| 切换 includeRisky | 列表项增减的 AnimatedVisibility |

### 5.3 Motion 代码模板（Compose）

```kotlin
// One UI 10 Basic Path Interpolator
val OneUIBasic = CubicBezierEasing(0.22f, 0.25f, 0.00f, 1.00f)

// 标准进入动画
fun Modifier.enterTransition() = this
    .animateContentSize(animationSpec = tween(300, easing = OneUIBasic))
    .fadeIn(animationSpec = tween(250, easing = OneUIBasic))

// 计数动画（数字滚动）
animateFloatAsState(
    targetValue = targetBytes,
    animationSpec = tween(durationMillis = 600, easing = OneUIBasic),
    label = "bytesCounter"
)

// 列表进入（stagger）
val visible = remember { mutableStateOf(false) }
LaunchedEffect(Unit) { visible.value = true }
// 每项：index * 30ms stagger，opacity 0→1，translateY 20→0
```

---

## 六、空状态与降级状态设计

### 6.1 分类空状态

One UI 10 规范：错误信息要**说清原因 + 给出行动**。

| 场景 | 标题 | 说明 | 行动 |
|------|------|------|------|
| 真干净 | "设备状态良好" | "已扫描 156 个应用，未发现值得清理的内容" | "重新扫描" |
| 权限不足 | "无法完成完整扫描" | "使用情况访问权限未开启，部分缓存无法识别" | "开启权限" |
| 引擎不可用 | "扫描功能暂时不可用" | "Rust 引擎未连接，仅显示系统基础读数" | "重试连接" |
| 网络问题（AI） | "AI 助手需要网络" | "当前处于离线模式，基础功能不受影响" | "忽略" |

### 6.2 降级感知设计

**原则**：降级时**不降体验**，告诉用户"现在能做什么"，而非"什么不能做"。

```
引擎不可用时：
┌─────────────────────────────────┐
│  🔴 扫描功能不可用              │
│                                 │
│  当前可使用：                    │
│  ✓ 查看存储占用                 │  ← 用户仍然有可用功能
│  ✓ 查看已装应用列表             │
│  ✓ 使用基础清理（引导模式）      │
│                                 │
│  [重试连接]                     │
└─────────────────────────────────┘
```

---

## 七、一键直达的权限引导设计

### 7.1 权限引导的时机

**现在**：缺失权限时始终渲染引导卡
**应当**：只在**影响当前操作时**渲染，且按影响程度排序

```
首页加载 → 扫描执行前检测权限
执行清理前 → 检测 ALL_FILES_ACCESS（必须）
冻结前 → 检测 USAGE_STATS（必须）
AI 助手 → 检测网络状态
```

### 7.2 引导卡设计

One UI 10 Quick Panel 规范：信息分区 + 独立按钮 + 一键触达

```
┌──────────────────────────────────────────┐
│ ⚠️ 使用情况访问权限                       │
│ 识别长期未用的应用，计算可清理空间        │
│                                          │
│                    [开启权限 →]            │
└──────────────────────────────────────────┘
```

引导完成后自动消失（`Lifecycle.Event.ON_RESUME` 时重查 `missingCapabilities()`）。

---

## 八、AI 助手交互（AssistantScreen）

### 8.1 当前的占位状态 → 应当的设计

**问题**：AssistantScreen 是占位，无实际对话能力

**应当**：

```
┌─────────────────────────────────┐
│  AI 助手                    [⚙️] │
├─────────────────────────────────┤
│                                  │
│  [AI 图标]  你好！有什么可以帮你 │
│                                  │
│  快速建议：                      │
│  [建议1] "帮我清理微信缓存"       │
│  [建议2] "哪些应用最占空间？"     │
│  [建议3] "解释一下扫描结果"       │
│                                  │
│  ─────────────────────────────  │
│  [ 输入框... ]           [发送] │
└─────────────────────────────────┘
```

**隐私保护**：对话全程本地 LLM（或用户自配 API Key），不默认联网。

---

## 九、技术实现路径

### 9.1 Compose Animation 改造清单

| 文件 | 改动 |
|------|------|
| `HomeScreen.kt` | 重构 hero 区域；权限引导卡按需渲染；添加扫描预告弹层 |
| `CleanScreen.kt` | 添加先结论后详情的 accordion 结构；执行过程可见化；重复文件专属区块 |
| `FreezeScreen.kt` | 已冻结/未冻结状态区分；Shizuku 引导流程 |
| `AuroraBackground.kt` | 增强动效（参考 One UI 极光色渐变） |
| 新增 `components/MotionCard.kt` | 统一的进入/退出动画封装 |
| 新增 `components/CountingNumber.kt` | 数字滚动动画组件 |

### 9.2 状态机补充

| 状态 | 触发 | 显示内容 |
|------|------|---------|
| `PreScan` | 点击"一键释放"后 | 扫描预告（不跑扫描，展示扫描范围） |
| `Executing` | 开始执行清理 | 执行进度（当前项 + 进度条 + 已完成项列表） |
| `NeedsReview` | 需手动处理时 | 引导用户去系统设置页，不计入 freedBytes |

### 9.3 动画性能规范

One UI 10 规范：使用 `transform` 和 `opacity` 做动画（GPU 加速）；避免在动画中修改布局参数。

```kotlin
// ✅ 正确：GPU 加速属性
Modifier.animateContentSize()
Modifier.alpha()

// ❌ 错误：触发重排
Modifier.layoutParams = ...
Modifier.padding(animatedValue)  // 改用 scale/alpha 替代
```

---

## 十、优先级与排期建议

### Phase 1（1-2 周）：核心体验补完
- 首页扫描预告弹层
- 执行过程可见化（Executing 状态）
- 空状态分类设计（真干净 / 权限不足 / 引擎不可用）
- 权限引导卡按需渲染（去掉首页常驻卡）

### Phase 2（2-3 周）：交互细节打磨
- 数字滚动计数动画
- Micro-interaction 清单（按钮反馈、勾选反馈）
- 重复文件专属区块
- 已冻结/未冻结状态区分

### Phase 3（3-4 周）：AI 助手与进阶
- AssistantScreen 对话式 UI
- 快速建议卡片
- 本地 LLM 接入（或 API Key 配置页）
- Shizuku 引导流程

---

*本文档为交互设计方案，实际实现需根据技术评审调整优先级。*
*设计依据：One UI 10 Design System (Samsung Developer)、Fluid AI Design System、Material 3 Compose Animation 规范。*
